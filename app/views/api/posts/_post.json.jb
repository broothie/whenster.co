{
  id: post.id,
  userID: post.user.id,
  eventID: post.event.id,
  body: post.body,
  createdAt: post.created_at,
  imageURLs: render(partial: "api/posts/image_urls", collection: post.images, as: :image),
}
