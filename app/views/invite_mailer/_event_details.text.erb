What
<%= event.title %>

When
<%= event.start_at.in_time_zone((defined?(user) && user.timezone) || event.timezone).strftime(long_time_format) %>

<% if event.location %>
Where
<%= event.location %>
<% end %>

Details
<%= event.description %>
