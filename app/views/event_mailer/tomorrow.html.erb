<div class="space-y-3 p-3">
  <p>Hey <%= @user.username %> 👋</p>

  <p><%= link_to @event.title, base_url("events", @event.id), class: "link" %> is tomorrow!</p>

  <%= render "invite_mailer/event_details", event: @event, user: @user %>
</div>
